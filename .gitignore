# Segredos / ambiente — NUNCA versionar (vêm de variáveis de ambiente)
.env
*.env

# Override de dev local (aponta p/ um .env fora do repo) — nunca versionar
config_local.py

# Locks e temporários do Excel (guardam nome de usuário)
~$*

# Python
__pycache__/
*.pyc

# Artefato temporário do processo de higienização do .xlam
*.clean.xlam
